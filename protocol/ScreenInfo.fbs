include "IPAddress.fbs";

table ScreenInfo {
	name: string;
	ipAddress: IPAddress;
	corePort: ushort = 24800;
	coreProtocolVersion: ubyte = 1;
	servicePort: ushort = 24801;
	serviceProtocolVersion: ubyte = 1;
}
