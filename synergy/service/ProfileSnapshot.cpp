#include "ProfileSnapshot.h"
//#include "json.hpp"

//DEFINE_JSON(Profile, (int, configVersion) (int, id) (std::string, name) (int, server));


//DEFINE_JSON(Screen, (int, id) (std::string, name) (std::string, active)
//                    (std::string, ipList) (std::string, status)
//                    (int, x_pos) (int, y_pos));

//DEFINE_JSON(JsonConfig, (Profile, profile) (std::vector<Screen>, screens));

void ProfileSnapshot::parseJsonSnapshot(std::string const &jsonConfig)
{
//    auto configJsonObject = json::from_string(jsonConfig);

//    JsonConfig profileObject;

//    from_json(profileObject, configJsonObject);
}
